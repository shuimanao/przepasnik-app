import React, { FC } from "react";
import { StyledInput } from "./styles";

// TODO: add logic for searching
export const SearchBar: FC = () => {
  return <StyledInput type="search" placeholder="Szukaj …" />;
};
